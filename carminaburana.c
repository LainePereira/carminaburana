#include "cs50.h"
#include "stdio.h"

int main(void) {
    char_nome = "Carmina Burana";

    int extenção = (nome);

    printf("Vogais '%s', nome);

    for (int i = 0; i < extenção; i++) {
        if (nome[i] = ('a' 'i' 'u') nome[i] = 'A' 'I' 'U' ) {
            printf("%c ", nome[i]);
        
    }

    printf("\n");
}
